<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Tippy" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- primary brand color. -->
        <item name="colorPrimary">@color/primary_orange</item>
        <!-- secondary brand color. -->
        <item name="colorSecondary">@color/secondary_orange</item>
        <!-- status bar color. -->
        <item name="android:statusBarColor">@color/secondary_orange</item>
        <!-- background color. -->
        <item name="android:colorBackground">@color/primary_orange</item>
    </style>

    <style name="Theme.Tippy" parent="Base.Theme.Tippy" />
</resources>